{
  "name": "sprint-demo-nodejs",
  "version": "0.1.0",
  "private": true,
  "description": "Scrum sprint simulation: online shop MVP",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec",
    "check": "node scripts/check.mjs",
    "coverage": "node --test --test-reporter=spec --experimental-test-coverage --test-coverage-include=src/*.js --test-coverage-exclude=src/demo.js",
    "demo": "node src/demo.js"
  },
  "license": "MIT"
}
